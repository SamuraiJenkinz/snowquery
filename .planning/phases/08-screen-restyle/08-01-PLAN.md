---
phase: 08-screen-restyle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/css.py
  - app.py
autonomous: false

must_haves:
  truths:
    - "Sidebar renders with #F5F0EB (or #EDE6DD) bg, 320px fixed width, no green/red brutalist colors anywhere in the rendered sidebar DOM"
    - "Top of sidebar shows SNOWGREP wordmark in EB Garamond 28px weight 300 charcoal #2C2420, letter-spacing 0.02em — no S-box, no green border"
    - "Four section headers — DATA, EMBEDDINGS, LLM PROVIDER, MODE — render as small-caps Inter 500 11-12px warm-gray #6B5E52, letter-spacing 0.1em; literal strings present verbatim in app.py"
    - "MODE is a three-button row (AUTO / SQL / SEMANTIC); selected pill has cashmere #8B7355 bg + white text; clicking a pill updates st.session_state['query_mode'] to one of the internal MODE_OPTIONS values ('auto', 'structured', 'semantic')"
    - "When st.session_state['_llm_provider_blocked'] == True, all three MODE pills render with opacity 0.5 + cursor not-allowed (mirrors css.py:334-343 disabled cashmere button pattern)"
    - "EMBEDDINGS pill renders sage 'READY' (#8A9A7D tint) when embeddings_exist() returns True, or terracotta 'MISSING' (#A67866 tint) when False — no green/amber brutalist dots"
    - "LLM PROVIDER selectbox shows bottom-border-only style (1px #E8E0D8 on bottom, 0 on other 3 sides) with locked label 'LLM provider' (verbatim) and active model in muted-gold #B8A88A Inter 11px tracked caption beneath"
    - "When missing_vars(provider) returns non-empty, a single warm-beige container with 3px terracotta #A67866 left border replaces st.warning(); top line reads 'WARNING — PROVIDER NOT CONFIGURED' verbatim in small-caps Inter 500 letter-spacing 0.1em; env var names render in <code> tags (Mono via Phase 6 boundary); st.session_state['_llm_provider_blocked'] still flips True"
    - "Brutalist HTML blocks at app.py:152-160 (terminal logo), 189-193 (red LOCKED chip), 200-204 (green UPLOAD UNLOCKED), 254-260 (red status dot), 270-274 (green DOCS INDEXED), 277-281 (amber NO EMBEDDINGS) are deleted — grep for '#00ff00', '#ff5f56', '#ffbd2e' in sidebar region returns zero matches"
    - "22/22 tests in tests/test_phase5_ui.py still pass — locked v2.1 strings ('LLM provider', 'Azure OpenAI', 'Anthropic Claude (MGTI)') preserved verbatim"
  artifacts:
    - path: "src/ui/css.py"
      provides: "Sidebar editorial selectors (wordmark, section headers, MODE pill, EMBEDDINGS pill, bottom-border-only selectbox, warning container)"
      contains: "/* === Sidebar (Phase 8 SBR-*) === */"
    - path: "app.py"
      provides: "Refactored render_sidebar() consuming Phase 6 tokens + Phase 8 sidebar selectors; deleted brutalist HTML"
      min_lines: 600
  key_links:
    - from: "app.py render_sidebar() MODE pill click handler"
      to: "st.session_state['query_mode']"
      via: "st.button + st.session_state write inside MODE section"
      pattern: "st\\.session_state\\[['\"]query_mode['\"]\\]\\s*="
    - from: "app.py render_sidebar() provider warning container"
      to: "missing_vars(provider) return + st.session_state['_llm_provider_blocked']"
      via: "if _missing: render warm-beige container; st.session_state['_llm_provider_blocked'] = True"
      pattern: "_llm_provider_blocked['\"]\\]\\s*=\\s*True"
    - from: "Sidebar CSS .lp-mode-pill[aria-pressed='true']"
      to: "src/ui/css.py token --lp-accent"
      via: "background: var(--lp-accent)"
      pattern: "lp-mode-pill.*--lp-accent"
---

<objective>
Restyle the entire sidebar — wordmark hero, four section headers (DATA, EMBEDDINGS, LLM PROVIDER, MODE), MODE pill toggle (replacing the legacy main-panel selectbox), EMBEDDINGS sage/terracotta status pill, bottom-border-only LLM PROVIDER selectbox, and a restyled provider-warning container — to the editorial Loro Piana pattern. Delete the brutalist inline `<div style="...">` HTML chunks Phase 6 surfaced. Behavior stays byte-identical to v2.1: same session_state keys, same locked strings, same blocked-state contract.

Purpose: Closes Wave A of Phase 8 (SBR-01..06) — the sidebar half of the editorial revamp. Establishes the MODE pill as the single source of truth for `st.session_state["query_mode"]` (the legacy main-panel selectbox at `app.py:684-699` is deleted by Wave B / Plan 02). Locks the small-caps tracked label pattern that Phase 10 will extend to loading indicators.

Output:
- `src/ui/css.py` extended with a new `/* === Sidebar (Phase 8 SBR-*) === */` section appended to the existing CSS string (no edits to Phase 6 selectors, append-only)
- `app.py::render_sidebar()` refactored: brutalist HTML deleted, four sections restructured with `<div class="lp-section">` wrappers, MODE pill row added, EMBEDDINGS pill replaces brutalist colored dots, LLM PROVIDER warning replaced with custom warm-beige container
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-screen-restyle/08-CONTEXT.md

# Visual contract — the binding mockup for the sidebar
@.planning/design-mockups/01-main-chat.png

# Canonical Loro Piana component patterns (sidebar, divider, status badge, input field, button)
@C:\Users\taylo\.claude\skills\loro-piana-aesthetic\references\component-patterns.md

# Phase 6 CSS module — read-only, extend by APPENDING after line 376
@src/ui/css.py

# Current sidebar code — full region to refactor
@app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append sidebar CSS section to src/ui/css.py</name>
  <files>src/ui/css.py</files>
  <action>
APPEND a new CSS section to the existing `LORO_PIANA_CSS` triple-quoted string in `src/ui/css.py`. Do NOT edit any existing selectors (Phase 6 contract: extend, don't modify). Insert the new block BEFORE the closing `"""` on line ~377 of the current file, after the existing `/* Inputs */` block at lines 358-376.

The new section header (verbatim, so checker can grep for it):

```
/* === Sidebar (Phase 8 SBR-*) === */
```

Selectors to add (all scoped under `[data-testid="stSidebar"]` so they don't bleed into the main panel):

**SBR-01 — Wordmark hero**

```css
[data-testid="stSidebar"] .lp-sidebar-wordmark {
  font-family: var(--lp-font-headline);
  font-weight: 300;
  font-size: 28px;
  color: var(--lp-text);
  letter-spacing: 0.02em;
  padding: var(--lp-space-6) 0 var(--lp-space-4) 0;
  margin: 0;
  line-height: 1;
}
```

Also fix sidebar width — append (still inside `[data-testid="stSidebar"]` scope):

```css
[data-testid="stSidebar"] {
  width: 320px !important;
  min-width: 320px !important;
  max-width: 320px !important;
}
[data-testid="stSidebar"] [data-testid="stSidebarContent"],
[data-testid="stSidebar"] > div:first-child {
  background: var(--lp-bg);  /* override Phase 6 --lp-neutral-100 to match mockup */
  padding-left: var(--lp-space-6);
  padding-right: var(--lp-space-6);
}
```

Pick `#F5F0EB` (--lp-bg) over `#EDE6DD` (--lp-neutral-100) to match mockup `.planning/design-mockups/01-main-chat.png`. If contrast against `--lp-border` becomes too low during Task 2 visual check, switch to `#EDE6DD` in this rule.

**SBR-02 — Section headers (small-caps tracked)**

```css
[data-testid="stSidebar"] .lp-section-header {
  font-family: var(--lp-font-body);
  font-weight: 500;
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--lp-text-muted);
  margin: 0 0 var(--lp-space-3) 0;
  padding: 0;
}
[data-testid="stSidebar"] .lp-section-rule {
  height: 1px;
  background: var(--lp-border);
  border: 0;
  margin: var(--lp-space-6) 0;
}
```

**SBR-03 — MODE pill toggle**

Pill row container + three buttons. Streamlit `st.button` keyed `mode_auto` / `mode_sql` / `mode_semantic` will be rendered inside three `st.columns` in Task 2. The selected pill is identified via a wrapper class set in Python (`<div class="lp-mode-pill lp-mode-pill--active">`) since `st.button` doesn't accept `aria-pressed`.

```css
[data-testid="stSidebar"] .lp-mode-pill-row {
  display: flex;
  gap: var(--lp-space-2);
  margin-top: var(--lp-space-2);
}
[data-testid="stSidebar"] .lp-mode-pill .stButton > button {
  width: 100%;
  background: transparent;
  color: var(--lp-text);
  border: 1px solid var(--lp-border);
  border-radius: var(--lp-radius-md);
  font-family: var(--lp-font-body);
  font-weight: 500;
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  padding: var(--lp-space-2) var(--lp-space-3);
  box-shadow: none;
  transition: var(--lp-transition-base);
}
[data-testid="stSidebar"] .lp-mode-pill .stButton > button:hover {
  background: var(--lp-neutral-100);
  color: var(--lp-text);
  border-color: var(--lp-border);
  box-shadow: none;
}
[data-testid="stSidebar"] .lp-mode-pill--active .stButton > button,
[data-testid="stSidebar"] .lp-mode-pill--active .stButton > button:hover {
  background: var(--lp-accent);
  color: var(--lp-neutral-0);
  border-color: var(--lp-accent);
}
[data-testid="stSidebar"] .lp-mode-pill--blocked .stButton > button,
[data-testid="stSidebar"] .lp-mode-pill--blocked .stButton > button:hover {
  opacity: 0.5;
  cursor: not-allowed;
}
```

**SBR-04 — EMBEDDINGS status pill**

```css
[data-testid="stSidebar"] .lp-status-pill {
  display: inline-block;
  font-family: var(--lp-font-body);
  font-weight: 500;
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  padding: var(--lp-space-1) var(--lp-space-3);
  border-radius: var(--lp-radius-full);
}
[data-testid="stSidebar"] .lp-status-pill--ready {
  background: rgba(138, 154, 125, 0.12);  /* #8A9A7D at ~12% */
  color: var(--lp-success);
}
[data-testid="stSidebar"] .lp-status-pill--missing {
  background: rgba(166, 120, 102, 0.12);  /* #A67866 at ~12% */
  color: var(--lp-danger);
}
```

**SBR-05 — Bottom-border-only LLM PROVIDER selectbox**

Streamlit's `st.selectbox` renders BaseWeb `[data-baseweb="select"]` inside `.stSelectbox`. We scope by sidebar + a wrapper class set in Python (`<div class="lp-bb-select">`) wrapping the `st.selectbox` call so we don't accidentally re-style other selectboxes.

```css
[data-testid="stSidebar"] .lp-bb-select .stSelectbox [data-baseweb="select"] {
  background: var(--lp-surface);
  border: 0;
  border-bottom: 1px solid var(--lp-border);
  border-radius: 0;
  box-shadow: none;
}
[data-testid="stSidebar"] .lp-bb-select .stSelectbox [data-baseweb="select"]:focus-within {
  border-bottom-color: var(--lp-accent);
  box-shadow: none;
}
[data-testid="stSidebar"] .lp-bb-select .stSelectbox label {
  font-family: var(--lp-font-body);
  font-size: 11px;
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--lp-text-muted);
}
[data-testid="stSidebar"] .lp-active-model {
  font-family: var(--lp-font-body);
  font-size: 11px;
  font-weight: 500;
  color: var(--lp-neutral-400);  /* #B8A88A muted gold */
  letter-spacing: 0.1em;
  text-transform: uppercase;
  margin-top: var(--lp-space-2);
}
```

**SBR-06 — Provider warning container**

```css
[data-testid="stSidebar"] .lp-warn-card {
  background: var(--lp-bg);
  border-left: 3px solid var(--lp-danger);
  border-top: 0;
  border-right: 0;
  border-bottom: 0;
  border-radius: 0 var(--lp-radius-md) var(--lp-radius-md) 0;
  padding: var(--lp-space-4);
  margin: var(--lp-space-3) 0;
}
[data-testid="stSidebar"] .lp-warn-card .lp-warn-label {
  font-family: var(--lp-font-body);
  font-weight: 500;
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--lp-danger);
  margin: 0 0 var(--lp-space-2) 0;
}
[data-testid="stSidebar"] .lp-warn-card .lp-warn-body {
  font-family: var(--lp-font-body);
  font-size: 14px;
  color: var(--lp-text);
  margin: 0 0 var(--lp-space-2) 0;
  line-height: 1.4;
}
[data-testid="stSidebar"] .lp-warn-card .lp-warn-fix {
  font-family: var(--lp-font-body);
  font-size: 13px;
  color: var(--lp-text-muted);
  margin: 0;
  line-height: 1.4;
}
[data-testid="stSidebar"] .lp-warn-card code {
  background: rgba(166, 120, 102, 0.08);
  padding: 1px 4px;
  border-radius: 2px;
  /* font-family inherits JetBrains Mono via Phase 6 mono boundary */
}
```

After appending, the entire CSS string remains a single Python triple-quoted constant — no syntax change, no new exports.
  </action>
  <verify>
    `python -c "from src.ui.css import LORO_PIANA_CSS; assert '/* === Sidebar (Phase 8 SBR-*) === */' in LORO_PIANA_CSS; assert '.lp-sidebar-wordmark' in LORO_PIANA_CSS; assert '.lp-section-header' in LORO_PIANA_CSS; assert '.lp-mode-pill--active' in LORO_PIANA_CSS; assert '.lp-mode-pill--blocked' in LORO_PIANA_CSS; assert '.lp-status-pill--ready' in LORO_PIANA_CSS; assert '.lp-status-pill--missing' in LORO_PIANA_CSS; assert '.lp-bb-select' in LORO_PIANA_CSS; assert '.lp-active-model' in LORO_PIANA_CSS; assert '.lp-warn-card' in LORO_PIANA_CSS; assert '.lp-warn-label' in LORO_PIANA_CSS; print('all sidebar selectors present')"`
    `python -c "from src.ui.css import LORO_PIANA_CSS, LORO_PIANA_TOKENS; print('module loads OK')"` — confirms no syntax error introduced
    `pytest tests/test_phase5_ui.py -q` — must remain 22/22 green (CSS changes are pure additions)
  </verify>
  <done>
    `src/ui/css.py` contains all 11 new selector groups under the verbatim `/* === Sidebar (Phase 8 SBR-*) === */` section marker. Module imports without error. Existing Phase 5 UI tests (22) still pass — pure CSS additions cannot affect them, but the run is the regression guard.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor render_sidebar() — delete brutalist HTML, restructure sections, add MODE pill + EMBEDDINGS pill + warning card</name>
  <files>app.py</files>
  <action>
Rewrite the body of `render_sidebar()` (currently `app.py:148-376`) to consume the Phase 8 sidebar selectors from Task 1. Do NOT change the function signature or move any code outside the `with st.sidebar:` block. Do NOT touch `_render_provenance_caption` (lines 65-87) — the AST invariant in `tests/test_phase5_ui.py` locks it.

**Step-by-step edits inside `with st.sidebar:`** (line numbers refer to the pre-edit state):

1. **DELETE lines 152-160** (brutalist terminal-logo div with `#00ff00` border + S-box + "SNOWGREP" + "INCIDENT QUERY"). REPLACE with the editorial wordmark:

```python
st.markdown(
    '<h1 class="lp-sidebar-wordmark">SNOWGREP</h1>',
    unsafe_allow_html=True,
)
st.markdown('<hr class="lp-section-rule" />', unsafe_allow_html=True)
```

2. **REPLACE the brutalist `### DATA INGEST` heading on line 162** with the new small-caps header. Then CONSOLIDATE the DATA INGEST + DATA STATUS sections (line 162 + line 234) into a single `DATA` section. Insert at the start of this combined section:

```python
st.markdown('<p class="lp-section-header">DATA</p>', unsafe_allow_html=True)
```

3. **DELETE the brutalist HTML chunks inside the DATA section**:
   - lines 189-193 (red 🔒 LOCKED chip) — REPLACE with nothing (the password input + UNLOCK UPLOAD button already convey lock state via cashmere button styling from Phase 6)
   - lines 200-204 (green 🔓 UPLOAD UNLOCKED div) — REPLACE with: `st.markdown('<p class="lp-label">UPLOAD UNLOCKED</p>', unsafe_allow_html=True)` (consumes the `.lp-label` utility already in `css.py:229-236`)
   - lines 254-260 (red status-dot NO DATA div) — REPLACE with: `st.markdown('<p class="lp-label" style="color: var(--lp-danger);">NO DATA LOADED</p>', unsafe_allow_html=True)`
   - line 234 (`st.markdown("### DATA STATUS")`) — DELETE (consolidated into single DATA section above)
   - line 162's `st.markdown("### DATA INGEST")` — DELETE (replaced by the single `lp-section-header` insert in step 2)
   - line 197's `st.caption("⚠️ USING DEFAULT PASSWORD")` — REPLACE with: `st.markdown('<p class="lp-label" style="color: var(--lp-warning);">USING DEFAULT PASSWORD</p>', unsafe_allow_html=True)`
   - line 214 `st.caption("DROP CSV FILE HERE")` — KEEP (existing v2.1 caption, no brutalist colors)

4. **REPLACE the divider on line 231 + 262**: use `st.markdown('<hr class="lp-section-rule" />', unsafe_allow_html=True)` instead of `st.divider()` so spacing is controlled by the CSS rule (24px above + below, no Streamlit defaults bleeding through).

5. **EMBEDDINGS section (lines 264-292)** — REPLACE the brutalist colored-dot divs (lines 270-274 green DOCS INDEXED, lines 277-281 amber NO EMBEDDINGS) with the new status pill:

```python
st.markdown('<p class="lp-section-header">EMBEDDINGS</p>', unsafe_allow_html=True)
if embeddings_exist():
    stats = get_embedding_stats()
    st.session_state.embeddings_ready = True
    st.markdown(
        f'<span class="lp-status-pill lp-status-pill--ready">READY · {stats["document_count"]:,} DOCS</span>',
        unsafe_allow_html=True,
    )
else:
    st.session_state.embeddings_ready = False
    st.markdown(
        '<span class="lp-status-pill lp-status-pill--missing">MISSING</span>',
        unsafe_allow_html=True,
    )
```

The REBUILD / UPDATE buttons (lines 283-291) — KEEP unchanged (already cashmere via Phase 6 button rules).

6. **LLM PROVIDER section (lines 295-349)** — wrap the selectbox + active-model caption in the `lp-bb-select` class so the bottom-border-only selector hits:

```python
st.markdown('<p class="lp-section-header">LLM PROVIDER</p>', unsafe_allow_html=True)

# ... existing default-resolution logic at lines 301-309 stays identical ...

st.markdown('<div class="lp-bb-select">', unsafe_allow_html=True)
selected_label = st.selectbox(
    "LLM provider",  # LOCKED v2.1 string — VERBATIM
    options=list(_PROVIDER_OPTIONS.keys()),
    index=_PROVIDER_KEYS.index(st.session_state["llm_provider"]),
    help="Which LLM serves classification, SQL generation, and executive summaries. Default is Azure OpenAI.",
)
st.session_state["llm_provider"] = _PROVIDER_OPTIONS[selected_label]
st.markdown('</div>', unsafe_allow_html=True)

# ... existing _settings / _active_model resolution at lines 326-332 stays identical ...

# Replace line 333 `st.caption(f"MODEL: ...")` with:
st.markdown(
    f'<p class="lp-active-model">MODEL · {(_active_model or "NOT CONFIGURED")}</p>',
    unsafe_allow_html=True,
)
```

LOCKED STRING GUARANTEE: the selectbox label MUST stay `"LLM provider"` byte-for-byte. The options list MUST stay `["Azure OpenAI", "Anthropic Claude (MGTI)"]` (driven by `_PROVIDER_OPTIONS` dict at app.py:57-60, do not touch that dict). `tests/test_phase5_ui.py` asserts these.

7. **REPLACE the `st.warning(...)` call at lines 342-346** with the editorial warning card:

```python
if _missing:
    _human_name = _PROVIDER_LABELS[st.session_state["llm_provider"]]
    _missing_code = ", ".join(f"<code>{v}</code>" for v in _missing)
    st.markdown(
        f'''<div class="lp-warn-card">
  <p class="lp-warn-label">WARNING — PROVIDER NOT CONFIGURED</p>
  <p class="lp-warn-body"><strong>{_human_name}</strong> is not configured. Missing: {_missing_code}.</p>
  <p class="lp-warn-fix">Add them to <code>.env</code> and restart, or switch provider above.</p>
</div>''',
        unsafe_allow_html=True,
    )
    st.session_state["_llm_provider_blocked"] = True
else:
    st.session_state["_llm_provider_blocked"] = False
```

LOCKED STRINGS in this card (Phase 11 TST-01 will pin them):
- `WARNING — PROVIDER NOT CONFIGURED` (verbatim, em-dash U+2014, single-word "configured" lowercase except the leading caps)
- Provider name (`Azure OpenAI` / `Anthropic Claude (MGTI)`) comes from `_PROVIDER_LABELS` and IS already locked

LOAD-BEARING: `st.session_state["_llm_provider_blocked"] = True/False` MUST stay written in the same order (set True inside the `if _missing` branch, False in `else`). The Phase 5 AST test in `tests/test_phase5_ui.py` doesn't check session_state order, but Phase 5 RESEARCH.md Pitfall 5 (render_sidebar() runs BEFORE render_main_content() so the flag is set before chat_input reads it) IS still in effect — `main()` at line 877 calls them in the right order, do NOT reorder.

Drop `icon=":material/warning:"` — the `<p class="lp-warn-label">` is the new visual indicator.

8. **ADD a new MODE section** between LLM PROVIDER and CONFIG (insert after the divider on line 351, before line 353's `### CONFIG`):

```python
st.markdown('<hr class="lp-section-rule" />', unsafe_allow_html=True)
st.markdown('<p class="lp-section-header">MODE</p>', unsafe_allow_html=True)

# Initialize query_mode session state. MODE_OPTIONS at app.py:46-51 stays the
# source of truth; pill exposes 3 modes (AUTO/SQL/SEMANTIC). HYBRID is dropped
# from the visible pill in v2.2 but internal "hybrid" value still works if
# anything else writes it. (Decision per CONTEXT.md "Open question for planner".)
if "query_mode" not in st.session_state:
    st.session_state["query_mode"] = "auto"  # MODE_OPTIONS["AUTO"] internal value

_blocked = st.session_state.get("_llm_provider_blocked", False)
_block_class = " lp-mode-pill--blocked" if _blocked else ""

_pill_specs = [
    ("AUTO",     "auto"),       # MODE_OPTIONS["AUTO"]
    ("SQL",      "structured"), # MODE_OPTIONS["REPORT [SQL]"]
    ("SEMANTIC", "semantic"),   # MODE_OPTIONS["SIMILAR [SEMANTIC]"]
]

st.markdown('<div class="lp-mode-pill-row">', unsafe_allow_html=True)
_pill_cols = st.columns(3)
for _col, (_label, _internal) in zip(_pill_cols, _pill_specs):
    with _col:
        _active = " lp-mode-pill--active" if st.session_state["query_mode"] == _internal else ""
        st.markdown(f'<div class="lp-mode-pill{_active}{_block_class}">', unsafe_allow_html=True)
        if st.button(_label, key=f"mode_pill_{_internal}", use_container_width=True, disabled=_blocked):
            st.session_state["query_mode"] = _internal
            st.rerun()
        st.markdown('</div>', unsafe_allow_html=True)
st.markdown('</div>', unsafe_allow_html=True)
```

This MOVES the MODE selector from main panel (`app.py:684-699` — deletion handled by Plan 02) to sidebar. The internal `"auto" / "structured" / "semantic"` values match `MODE_OPTIONS` exactly (per roadmap SC #1: "updates query_mode to the exact same value the legacy st.selectbox produced"). HYBRID is intentionally dropped from the visible pill per CONTEXT.md decisions.

9. **CONFIG section (lines 353-376)** — REPLACE only the `### CONFIG` heading on line 355 with `st.markdown('<p class="lp-section-header">CONFIG</p>', unsafe_allow_html=True)`. KEEP the `st.expander("QUERY SETTINGS")` and its three inner widgets (RESULTS LIMIT slider, SHOW SQL checkbox, EXEC SUMMARY checkbox) untouched — they inherit cashmere/editorial styling from Phase 6.

10. **VERIFY no brutalist hex colors remain in render_sidebar()**: grep the body of `render_sidebar` for `#00ff00`, `#ff5f56`, `#ffbd2e`, `#0a0a0a`, `#666` — must return zero matches.

**Constraint:** Do NOT modify `_render_provenance_caption` (lines 65-87) or `_PROVIDER_OPTIONS` / `_PROVIDER_LABELS` / `_PROVIDER_KEYS` (lines 57-62). Do NOT modify `MODE_OPTIONS` dict (lines 46-51). Do NOT modify the order of `render_sidebar()` and `render_main_content()` calls in `main()`.
  </action>
  <verify>
    Run static checks:
    - `python -m py_compile app.py` — must succeed
    - `grep -n "00ff00\|ff5f56\|ffbd2e\|0a0a0a" app.py` (or equivalent Grep tool) — must return zero matches inside the `render_sidebar` body (lines ~148-380 of the new file). Any hits outside that range are not Wave A's concern (Wave B / Plan 02 owns the main-panel cleanup).
    - `grep -n '"SNOWGREP"' app.py` — must show the wordmark string inside `render_sidebar`.
    - `grep -n "WARNING — PROVIDER NOT CONFIGURED" app.py` — must show exactly one verbatim match (the em-dash is U+2014, not two hyphens).
    - `grep -n 'class="lp-mode-pill' app.py` — must show three pill renders inside the MODE block.
    - `grep -n 'st.session_state\["_llm_provider_blocked"\]' app.py` — must show TWO writes inside render_sidebar (True branch + False branch) and at least one read inside render_main_content (the existing one at line 708 stays).
    - `grep -n '"LLM provider"' app.py` — must show exactly one verbatim match (the locked selectbox label).

    Run Phase 5 regression suite:
    - `pytest tests/test_phase5_ui.py -v` — must remain 22/22 green. Specifically watch the tests that assert the `"LLM provider"` label, the `Azure OpenAI` / `Anthropic Claude (MGTI)` option strings, and the AST invariant for `_render_provenance_caption`.

    Run a quick smoke import:
    - `python -c "import app"` — must succeed without raising (Streamlit's `st.set_page_config` etc. tolerate non-runtime import in this codebase since Phase 6).
  </verify>
  <done>
    `render_sidebar()` renders the editorial sidebar with:
    - Wordmark `<h1 class="lp-sidebar-wordmark">SNOWGREP</h1>` at top
    - Four `<p class="lp-section-header">` headers (DATA, EMBEDDINGS, LLM PROVIDER, MODE) plus CONFIG preserved
    - `<hr class="lp-section-rule">` between sections
    - EMBEDDINGS pill rendering sage READY / terracotta MISSING
    - LLM PROVIDER selectbox wrapped in `lp-bb-select` div with locked `"LLM provider"` label
    - Active model caption in `<p class="lp-active-model">`
    - Warm-beige warning card (`<div class="lp-warn-card">`) replaces `st.warning()` when `missing_vars` non-empty, with verbatim `WARNING — PROVIDER NOT CONFIGURED` label
    - Three-button MODE pill row writing `st.session_state["query_mode"]` with internal values `"auto" / "structured" / "semantic"`
    - When `_llm_provider_blocked` True, pills get `lp-mode-pill--blocked` class + `disabled=True`

    Zero brutalist hex colors (`#00ff00`, `#ff5f56`, `#ffbd2e`) in the `render_sidebar` body. 22/22 Phase 5 UI tests still green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify sidebar against design-mockup 01-main-chat.png</name>
  <what-built>
Wave A sidebar restyle complete:
- SBR-01 wordmark hero (EB Garamond 28px charcoal)
- SBR-02 four small-caps section headers (DATA, EMBEDDINGS, LLM PROVIDER, MODE) plus preserved CONFIG
- SBR-03 three-button MODE pill (AUTO / SQL / SEMANTIC) with cashmere selected state + blocked-state opacity
- SBR-04 EMBEDDINGS sage READY / terracotta MISSING pill
- SBR-05 bottom-border-only LLM PROVIDER selectbox + muted-gold active model caption
- SBR-06 warm-beige + 3px terracotta-left-border warning card with `WARNING — PROVIDER NOT CONFIGURED` label
- All six listed brutalist HTML chunks deleted (lines 152-160, 189-193, 200-204, 254-260, 270-274, 277-281)
  </what-built>
  <how-to-verify>
1. Run `streamlit run app.py` from the project root.
2. Open the app in a browser; wait for splash to clear.
3. **Compare sidebar against `.planning/design-mockups/01-main-chat.png`** at 1:1 (open the PNG in another window). Check:
   - "SNOWGREP" wordmark renders in EB Garamond, weight 300, ~28px, charcoal (NOT green, NOT bold, NOT in a box)
   - Section headers DATA / EMBEDDINGS / LLM PROVIDER / MODE / CONFIG render in small-caps Inter, tracked ~0.1em, warm-gray (NOT green, NOT all-caps brutalist `###`)
   - Hairline rules separate each section (1px warm-beige, 24px padding above + below)
   - Sidebar bg is warm off-white (#F5F0EB); no green/red brutalist colors anywhere
4. **MODE pill interaction**:
   - Click AUTO → AUTO pill goes cashmere bg + white text; SQL + SEMANTIC stay transparent + charcoal text
   - Click SQL → SQL becomes cashmere; AUTO + SEMANTIC un-fill
   - Click SEMANTIC → SEMANTIC becomes cashmere
   - In DevTools, run `JSON.parse(sessionStorage.getItem('_st_session_state') || '{}')` is unreliable; instead, after each click, run a query and confirm the response indicates the right mode (or temporarily print `st.session_state.query_mode` in Python — but undo before commit)
5. **EMBEDDINGS pill**:
   - If embeddings exist locally: pill shows sage "READY · N DOCS" (NOT green dot)
   - If you run `rm chroma_db/*.sqlite3` (or however your env clears embeddings) and reload: pill shows terracotta "MISSING"
6. **LLM PROVIDER selectbox**:
   - DevTools: inspect the `[data-baseweb="select"]` — must show `border-bottom: 1px solid` warm-beige and `border-top/right/left: 0` (or none)
   - Label reads `LLM provider` (verbatim, no caps changes)
   - Active model caption below renders in muted gold (#B8A88A), small-caps, Inter 11px
7. **Provider warning** (force by unsetting one env var, e.g. `unset ANTHROPIC_API_KEY` if Anthropic is selected, then reload):
   - Warm-beige container with terracotta 3px left border, no other borders
   - Top line reads `WARNING — PROVIDER NOT CONFIGURED` verbatim, small-caps Inter
   - Body line: provider human-name bold, env var names rendered in JetBrains Mono (the `<code>` tags pick up Phase 6 mono boundary)
   - Fix line: muted-gray Inter 13px
   - In sidebar inspect: `st.session_state._llm_provider_blocked` flips True (confirm by checking that the chat input on the main panel shows `QUERY DISABLED — see sidebar warning` — that's the cross-check, but the chat input STYLE is Wave B's territory; just confirm the state flips)
8. **Blocked MODE pills**: with provider missing, the three pills go opacity 0.5 + `cursor: not-allowed` on hover, AND clicking them does NOT change the active pill
9. Run `pytest tests/test_phase5_ui.py -v` one final time — must report 22/22 passed.

If ANY of the above checks fail, describe what you see vs what the mockup shows, and Claude will fix it before this checkpoint is approved. If all pass, confirm with "approved".
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific visual/functional discrepancies vs. `.planning/design-mockups/01-main-chat.png`.</resume-signal>
</task>

</tasks>

<verification>
- All six SBR-* requirements (SBR-01..06) observable in browser against `01-main-chat.png` mockup
- Phase 5 regression suite (22 tests) still green
- Zero brutalist hex colors (`#00ff00`, `#ff5f56`, `#ffbd2e`, `#0a0a0a`) in `render_sidebar` body
- Locked v2.1 strings preserved: `"LLM provider"`, `"Azure OpenAI"`, `"Anthropic Claude (MGTI)"` all grep-match
- New locked v2.2 string present verbatim: `"WARNING — PROVIDER NOT CONFIGURED"`
- `_render_provenance_caption` (lines 65-87) untouched (file diff confirms zero lines changed in that range)
- `MODE_OPTIONS` dict (lines 46-51) untouched (HYBRID internal value preserved for backward compat even though pill drops it)
- `st.session_state["_llm_provider_blocked"]` flag still written by `render_sidebar()` before `render_main_content()` reads it (function call order in `main()` untouched)
</verification>

<success_criteria>
Plan 01 complete when:
- [ ] `src/ui/css.py` contains `/* === Sidebar (Phase 8 SBR-*) === */` section with 11+ new selectors
- [ ] `app.py::render_sidebar()` consumes those selectors and drops all six listed brutalist HTML chunks
- [ ] MODE pill writes `st.session_state["query_mode"]` to `"auto" / "structured" / "semantic"` matching MODE_OPTIONS internal values
- [ ] EMBEDDINGS pill renders sage READY or terracotta MISSING based on `embeddings_exist()`
- [ ] LLM PROVIDER selectbox renders bottom-border-only with locked label `"LLM provider"`
- [ ] Provider warning card renders verbatim `WARNING — PROVIDER NOT CONFIGURED` label with 3px terracotta left border
- [ ] `_llm_provider_blocked` session_state contract preserved (True when missing, False otherwise)
- [ ] All 22 Phase 5 UI tests green
- [ ] Human-verify checkpoint approved against `01-main-chat.png`
</success_criteria>

<output>
After completion, create `.planning/phases/08-screen-restyle/08-01-SUMMARY.md` capturing:
- Confirmed SBR-01..06 implementations + observable behaviors
- Final selector count + section marker in `src/ui/css.py`
- Final `render_sidebar()` line-range vs. pre-edit
- Confirmation that MODE pill values write `st.session_state["query_mode"]` matching MODE_OPTIONS
- Confirmation that 22/22 Phase 5 UI tests stayed green throughout
- Any deviations from this plan + rationale
- Carry-forward decisions (e.g., final choice between `#F5F0EB` vs `#EDE6DD` for sidebar bg + warning card bg)
</output>
