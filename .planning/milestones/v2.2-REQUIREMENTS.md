# Requirements Archive: v2.2 SNOWGREP Visual Revamp (Loro Piana)

**Archived:** 2026-05-24
**Status:** ✅ SHIPPED

This is the archived requirements specification for v2.2.
For current requirements, see `.planning/REQUIREMENTS.md` (created for next milestone).

---

**Defined:** 2026-05-22
**Core Value:** Operators get accurate, fast natural-language answers about ServiceNow incidents using the LLM they choose — without their incident data ever leaving the box.
**Milestone goal:** Replace the brutalist terminal CSS with an editorial Loro Piana quiet luxury aesthetic across all screens, without touching v2.1 LLM behavior or losing any existing functionality.

**Design reference:**
- Three Stitch mockups in `.planning/design-mockups/` (00-splash-helix.png, 01-main-chat.png, 02-results-chart.png) — copies also at `docs/screenshots/`
- Live Stitch project: https://stitch.withgoogle.com/projects/11615568135320819515
- Design system: `loro-piana-aesthetic` skill — palette, tokens, components, makeover-checklist

## v1 Requirements (all shipped)

### Foundation (FND) — Design tokens, fonts, palette

- [x] **FND-01**: Google Fonts imports for EB Garamond (weights 300, 400) and Inter (weights 400, 500) are added to the global CSS injection in `app.py`. JetBrains Mono retained for code/data only. ✓ shipped Phase 6
- [x] **FND-02**: Streamlit base CSS color variables overridden via `:root` rules — `--background-color: #F5F0EB` (warm off-white), `--primary-color: #8B7355` (cashmere brown), `--text-color: #2C2420` (charcoal), `--secondary-background-color: #FFFFFF` (white surface). No `#0a0a0a` brutalist background appears anywhere in the rendered DOM. ✓ shipped Phase 6
- [x] **FND-03**: Existing brutalist CSS block (`app.py:44-` to closing `</style>`) is replaced with a Loro Piana CSS module (`src/ui/css.py`) that exports a single `LORO_PIANA_CSS` constant injected once via `st.markdown(..., unsafe_allow_html=True)`. No `font-family: 'JetBrains Mono', 'Courier New', monospace` on the global `.stApp` selector. ✓ shipped Phase 6
- [x] **FND-04**: All section labels render in Inter weight 500, UPPERCASE, `letter-spacing: 0.1em`, 11-12px, color `#6B5E52` (warm gray) — small-caps boutique-signage style. Verified via DOM grep for the label CSS class. ✓ shipped Phase 6
- [x] **FND-05**: Page chrome refresh — `st.set_page_config(page_icon=...)` value changed from brutalist `▣` to `✦` (U+2726 BLACK FOUR POINTED STAR). `page_title="SNOWGREP"` preserved. ✓ shipped Phase 6
- [x] **FND-06**: All buttons (`st.button`, `st.chat_input` submit) render with cashmere brown `#8B7355` background, white text, 4px border-radius, uppercase tracked label (`letter-spacing: 0.1em`). Verified on at least 3 distinct button instances. ✓ shipped Phase 6

### Splash (SPL) — Boot animation

- [x] **SPL-01**: `src/ui/splash.py` exports a `render_splash()` function that emits a self-contained `streamlit.components.v1.html(...)` block with the helix-motif CSS keyframes — two diagonal streams of INC IDs animating slowly with opacity fades, EB Garamond wordmark anchored center in protected whitespace exclusion zone. ✓ shipped Phase 7
- [x] **SPL-02**: Splash renders in an `st.empty()` placeholder at the top of `main()` and clears once `data_loaded` AND `embeddings_ready` are both true in `st.session_state`. Total visible duration capped at 4 seconds maximum even if data load is faster (anti-flash). ✓ shipped Phase 7
- [x] **SPL-03**: Helix animation respects `prefers-reduced-motion: reduce` — when set, INC IDs fade in/out at fixed positions instead of translating (no horizontal motion). Wordmark and labels render identically in both modes. ✓ shipped Phase 7
- [x] **SPL-04**: Splash is shown ONCE per browser session (tracked via `st.session_state['_splash_shown']` flag). Subsequent reruns within the session skip the splash. New browser session re-shows it. ✓ shipped Phase 7

### Sidebar (SBR) — Editorial sidebar restyle

- [x] **SBR-01**: Sidebar background is `#F5F0EB` (warm off-white) with 320px fixed width. SNOWGREP wordmark renders at top (delivered as branded PNG logo per approved deviation; `static/snowgrep-logo.png` served via Streamlit static serving). ✓ shipped Phase 8
- [x] **SBR-02**: Section headers — DATA, EMBEDDINGS, LLM PROVIDER, MODE — render as small-caps Inter weight 500, `letter-spacing: 0.1em`, 11-12px, color `#6B5E52`. Verbatim string match required. ✓ shipped Phase 8
- [x] **SBR-03**: MODE selector replaces the existing `st.selectbox` with a three-button pill row "AUTO" / "SQL" / "SEMANTIC". *Approved deviation*: delivered as `st.radio(horizontal=True)` with sage active-dot via `:has(input:checked)` selector — same contract (writes `query_mode` to `auto/structured/semantic`), cleaner active-state. ✓ shipped Phase 8
- [x] **SBR-04**: EMBEDDINGS status renders as a pill — "READY" in sage tint (`#8A9A7D` at 10% opacity background, `#8A9A7D` text) when embeddings exist; "MISSING" in terracotta tint (`#A67866` at 10% opacity bg, `#A67866` text) when not. ✓ shipped Phase 8
- [x] **SBR-05**: LLM PROVIDER dropdown (`st.selectbox` labeled "LLM provider") restyled — thin bottom border only (`border-bottom: 1px solid #E8E0D8`), no full box, white background. Active model name renders directly beneath in muted gold `#B8A88A` Inter 11px tracked. The locked v2.1 label string "LLM provider" stays verbatim (Phase 5 test asserts this). ✓ shipped Phase 8
- [x] **SBR-06**: Provider-warning state (missing env vars) restyles — warning box with warm-beige background `#F5F0EB`, terracotta `#A67866` thin left border (3px), `WARNING — PROVIDER NOT CONFIGURED` heading in small-caps tracked, body copy in Inter `#2C2420`. The locked v2.1 string "QUERY DISABLED — see sidebar warning" placeholder is preserved verbatim on `st.chat_input` when blocked. ✓ shipped Phase 8

### Main Panel (MAIN) — Chat + page chrome

- [x] **MAIN-01**: Page header renders "Incident Intelligence" in EB Garamond 36px weight 300 charcoal, with subtitle "Ask in natural language. All data stays local." in Inter 15px warm-gray `#6B5E52` directly below. *Approved deviation*: hero rendered as branded SNOWGREP PNG logo (alt text carries "Incident Intelligence" for accessibility); subtitle preserved verbatim. ✓ shipped Phase 8
- [x] **MAIN-02**: User messages in chat history render as warm-beige `#F5F0EB` cards with 4px border-radius, aligned right (max-width 70%, `margin-left: auto`). No avatar / role label. ✓ shipped Phase 8
- [x] **MAIN-03**: Assistant messages render as white `#FFFFFF` cards with `1px solid #E8E0D8` border, 4px border-radius, aligned left (max-width 85%). Soft warm shadow `0 1px 2px rgba(139,115,85,0.06)` permitted; no harsh box-shadow. ✓ shipped Phase 8
- [x] **MAIN-04**: Provenance caption (`_render_provenance_caption(provider, model)`) restyles to render `VIA <PROVIDER> · <model>` in Inter weight 500 small-caps `letter-spacing: 0.1em`, color muted gold `#B8A88A`, 11px. **The v2.1 invariant carries forward: the helper MUST NOT read `st.session_state` — history messages keep original provenance after mid-session provider switches.** AST-based regression test in `tests/test_phase5_ui.py` must still pass. ✓ shipped Phase 8; AST invariant preserved
- [x] **MAIN-05**: `st.chat_input` restyled — thin bottom border only (`border-bottom: 1px solid #E8E0D8`), placeholder "Ask anything about your incidents…" in Inter 15px warm-gray. Submit button renders the cashmere brown "ASK" pattern from FND-06. ✓ shipped Phase 8
- [x] **MAIN-06**: The v2.1 chat-input blocked state preserved verbatim — when `_llm_provider_blocked=True`, `st.chat_input(disabled=True)` with placeholder "QUERY DISABLED — see sidebar warning". Locked string from v2.1 Phase 5 tests. ✓ shipped Phase 8

### Data Visualization (DVZ) — Editorial dataframe + charts

- [x] **DVZ-01**: New helper `_render_editorial_table(df: pd.DataFrame) -> str` *(delivered in `src/ui/results.py`, not `src/utils.py` — approved deviation)* returns an HTML table string with: italic priority labels in cells where the column name matches "priority" (case-insensitive), warm-beige `#E8E0D8` 1px row dividers, no zebra stripes, no vertical column borders, EB Garamond serif column headers in small-caps tracked, generous cell padding (16px vertical, 24px horizontal), warm-beige background only on header row. ✓ shipped Phase 9
- [x] **DVZ-02**: Every assistant response that returns a DataFrame renders the editorial HTML table FIRST (as the hero view, matches mockup), followed by a single `st.expander("EXPAND · INTERACTIVE VIEW", expanded=False)` containing the full native `st.dataframe(df, use_container_width=True, hide_index=True)` plus a CSV download button. Zero functionality loss vs. v2.1. ✓ shipped Phase 9
- [x] **DVZ-03**: When the result DataFrame has >50 rows *(user-approved deviation from REQUIREMENTS.md `>1000` — editorial table is the hero view and must be scannable)*, the editorial HTML table shows only the first 50 rows with a small-caps tracked caption beneath: "SHOWING 50 OF <N> ROWS  ·  EXPAND BELOW FOR FULL DATA" (comma-formatted N, U+00B7 middot). The `st.dataframe` inside the expander always renders the full frame. ✓ shipped Phase 9
- [x] **DVZ-04**: New `src/ui/altair_theme.py` registers a custom Altair theme `loro_piana` (via `@alt.theme.register("loro_piana", enable=True)` decorator — Altair 6 API, NOT deprecated `alt.themes.*` namespace) with: cashmere graduated palette for bar marks, warm-beige `#E8E0D8` gridlines at 1px, no axis box stroke, EB Garamond chart titles 20px weight 300 color `#2C2420`, Inter axis labels 11px warm-gray `#6B5E52`. ✓ shipped Phase 9
- [x] **DVZ-05**: All chart rendering call sites (`src/chart_generator.py::generate_chart`) consume the new theme — `CHART_COLORS` and `configure_chart_theme` deleted; `VIBRANT_PALETTE` imported from `altair_theme.py`. Bar charts rewritten horizontal with layered `mark_text` value labels (Inter 12px charcoal, dx=4, align=left). Single-series charts have no legend (`legend=None`); pie retains legend; line uses crimson `#C0392B`. ✓ shipped Phase 9

### Polish (POL) — Edge states + indicators

- [x] **POL-01**: Empty state (no CSV loaded) renders an editorial centered card in the main panel — EB Garamond 24px "No data loaded" heading, Inter 15px warm-gray subtitle "Upload incidents.csv from the sidebar to begin.", and a hairline divider beneath. No brutalist "DATA INGEST REQUIRED" message. ✓ shipped Phase 10
- [x] **POL-02**: Loading spinners — `st.spinner` calls swap to a small-caps tracked text indicator. 4 callsites: LOADING DATA, BUILDING EMBEDDINGS ×2, ANALYZING. Inter weight 500 11px muted gold `#B8A88A` tracked wide with `@keyframes lp-pulse` animation. ✓ shipped Phase 10
- [x] **POL-03**: Error rendering — `QueryError` / `LLMError` surface in the assistant card with: terracotta `#A67866` left border 3px, "ERROR" small-caps tracked label at top, error message in Inter weight 400 color `#2C2420`, no red brutalist banner. 6 callsites covering all process_query error paths + outer try/except wrapper. XSS-safe via `html.escape()` on both `msg` and `label`. ✓ shipped Phase 10
- [x] **POL-04**: Toast / notification rendering — any `st.success` / `st.error` / `st.warning` calls restyle to the Loro Piana palette via CSS overrides on `stAlertContainer`, `stAlertContentSuccess|Error|Warning|Info`, `stAlertDynamicIcon`. No browser-default colors leak through. Zero call-site edits required. ✓ shipped Phase 10

### Documentation (DOC) — User-facing docs

- [x] **DOC-01**: USER_GUIDE.md gains a new section "VISUAL REFRESH (v2.2)" near the top with: one-paragraph summary of the aesthetic change, 5-bullet "What changed" list (splash, sidebar style, editorial table, expandable interactive view, restyled charts), 4-bullet "What did NOT change" reassurance list (all v2.1 functionality, locked UI strings, LLM behavior, data privacy). Version stamp footer bumped from "v2.1" to "v2.2". *Content accuracy refined in Phase 12*: expander caption corrected to "EXPAND · INTERACTIVE VIEW" (U+00B7); chart palette description corrected to full VIBRANT_PALETTE hex values. ✓ shipped Phase 11; refined Phase 12
- [x] **DOC-02**: README.md gains a "Screenshots" subsection between Features and Tech Stack linking to `docs/screenshots/` (byte-identical PNG copies from `.planning/design-mockups/`). Reference to the loro-piana-aesthetic design system noted in passing. *Content accuracy refined in Phase 12*: expander caption corrected; placeholder `https://github.com/` link removed (loro-piana-aesthetic kept as inline code). ✓ shipped Phase 11; refined Phase 12

### Testing (TST) — Regression coverage

- [x] **TST-01**: All 22 tests in `tests/test_phase5_ui.py` (v2.1 Phase 5 acceptance gate) stay green. The AST-based session_state-invariant test on `_render_provenance_caption` is load-bearing. Combined Phase 1+2+3+4+5 suite remains 91/91. ✓ shipped Phase 11 (22/22 + 91/91 green)
- [x] **TST-02**: New `tests/test_phase6_visual.py` acceptance gate proving v2.2 success criteria with zero live Streamlit / HTTP / LLM: CSS string presence checks (EB Garamond import, Inter import, palette tokens `#8B7355` / `#F5F0EB` / `#2C2420`); absence checks (no `#0a0a0a`, no `JetBrains Mono` on `.stApp`); helper function signature checks (`_render_editorial_table`, `render_splash`, Altair theme `loro_piana` registered). ✓ shipped Phase 11 (12/12 green)
- [x] **TST-03**: Accessibility contrast check — programmatic verification that `#2C2420` on `#F5F0EB` passes WCAG AA for body text (4.5:1 minimum; measured 13.4363) and `#6B5E52` on `#F5F0EB` passes AA for large text only (3:1 minimum; measured 5.5393). Inline sRGB linearization helper; negative usage scan over `var(--lp-text-muted)` rules with role-marker contract (uppercase | letter-spacing ≥ 0.1em | font-size ≥ 14px). ✓ shipped Phase 11

## v2 Requirements (deferred to v2.3+)

Tracked but not delivered in v2.2.

### Theming (THM)

- **THM-01**: Theme toggle in sidebar to switch between Loro Piana (default) and brutalist (preserved as alternate). Needs decision on whether to keep brutalist CSS as a callable alternate or fully retire it.
- **THM-02**: Dark-mode luxury variant — same warm earth tones at lower luminance for low-light operator environments.

### Microinteractions (MIC)

- **MIC-01**: Card hover states — subtle warm-beige shadow lift on assistant message cards.
- **MIC-02**: Provider switch transition — fade between sidebar widget states when provider dropdown changes.

### Charts (CHT)

- **CHT-01**: Sparkline column in editorial table for time-series fields.
- **CHT-02**: Interactive crossfilter between editorial table and chart (click a bar → filter the table).

## Out of Scope (v2.2)

Explicitly excluded for v2.2. Documented to prevent scope creep.

| Feature | Reason |
|---------|--------|
| Backend / LLM behavior changes | v2.2 was presentation only; LLM seam from v2.1 stayed untouched |
| Mobile responsive layout | Streamlit is desktop-first; no in-house mobile use case yet |
| Backwards-compatibility shim for brutalist theme | Theme toggle is v2 work (THM-01); v2.2 replaced brutalist cleanly |
| Porting `app_brutalist.py` / `fixedapp.py` / `designui.py` variants | They are experimental siblings; not in scope |
| Streamlit version upgrade | Stayed on current pin; CSS selectors target current DOM only |
| Custom Streamlit components (React) for widgets | Native `st.*` + CSS injection was sufficient; building custom components is 10x the scope |
| New LLM call sites or routing changes | v2.1 architecture locked |
| Animations beyond the splash | Splash is the one motion concession to "quiet luxury"; everything else is still |
| Font subsetting / self-hosted fonts | Google Fonts CDN was acceptable; subsetting is a perf-pass for v2 |
| Theme tokens as a published design system module | Tokens stay inline in `src/ui/css.py`; export as a package only when a second app needs them |

## Traceability (Final)

| Requirement | Phase | Status |
|-------------|-------|--------|
| FND-01 | 6  | Complete |
| FND-02 | 6  | Complete |
| FND-03 | 6  | Complete |
| FND-04 | 6  | Complete |
| FND-05 | 6  | Complete |
| FND-06 | 6  | Complete |
| SPL-01 | 7  | Complete |
| SPL-02 | 7  | Complete |
| SPL-03 | 7  | Complete |
| SPL-04 | 7  | Complete |
| SBR-01 | 8  | Complete (approved deviation: branded PNG logo) |
| SBR-02 | 8  | Complete |
| SBR-03 | 8  | Complete (approved deviation: `st.radio(horizontal=True)` with sage dot) |
| SBR-04 | 8  | Complete |
| SBR-05 | 8  | Complete |
| SBR-06 | 8  | Complete |
| MAIN-01 | 8 | Complete (approved deviation: branded PNG hero) |
| MAIN-02 | 8 | Complete |
| MAIN-03 | 8 | Complete |
| MAIN-04 | 8 | Complete (AST invariant preserved) |
| MAIN-05 | 8 | Complete |
| MAIN-06 | 8 | Complete |
| DVZ-01 | 9  | Complete (in `src/ui/results.py`, not `src/utils.py`) |
| DVZ-02 | 9  | Complete |
| DVZ-03 | 9  | Complete (approved deviation: 50-row truncation cap, not 1000) |
| DVZ-04 | 9  | Complete |
| DVZ-05 | 9  | Complete |
| POL-01 | 10 | Complete |
| POL-02 | 10 | Complete |
| POL-03 | 10 | Complete |
| POL-04 | 10 | Complete |
| DOC-01 | 11 | Complete (content accuracy refined Phase 12) |
| DOC-02 | 11 | Complete (content accuracy refined Phase 12) |
| TST-01 | 11 | Complete (22/22 green throughout) |
| TST-02 | 11 | Complete (12/12 green) |
| TST-03 | 11 | Complete (WCAG-AA verified inline) |

**Coverage:**
- v1 requirements: 36 total
- Shipped: 36/36 ✓
- Unmapped: 0 ✓

---

## Milestone Summary

**Shipped:** 36 of 36 v1 requirements
**Adjusted (approved deviations):**
- SBR-01 / MAIN-01: branded SNOWGREP PNG logo instead of EB Garamond text wordmark (single asset at `static/snowgrep-logo.png`)
- SBR-03: `st.radio(horizontal=True)` with `:has(input:checked)` sage dot instead of three `st.button` pills
- DVZ-01: Renderer lives in `src/ui/results.py` (not `src/utils.py`) alongside `css.py`, `splash.py`, `altair_theme.py`
- DVZ-03: 50-row truncation cap (not 1000) — editorial table is the hero view, must be scannable at a glance

**Dropped:** None
**Refined post-audit:** DOC-01, DOC-02 — content accuracy under existing requirements (Phase 12 closed audit doc-drift)

---
*Archived: 2026-05-24 as part of v2.2 milestone completion. Requirements defined: 2026-05-22.*
