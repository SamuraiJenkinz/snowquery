---
phase: quick-260621-aj0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_html_export.py
autonomous: true
requirements: [QUICK-260621-AJ0]
must_haves:
  truths:
    - "build_html_report escapes injected HTML in question, cells, and summary (no raw <script> survives; &lt;script&gt; present)"
    - "build_html_report promotes **bold** in the executive summary to <strong>"
    - "build_html_report renders the empty-DataFrame path without error (no-results message present)"
    - "generate_export_filename(prefix, 'html') yields a .html filename; the default extension still yields .csv (backward-compat lock)"
    - "html_report_to_bytes returns bytes"
  artifacts:
    - path: "tests/test_html_export.py"
      provides: "Regression test locking the HTML-export helpers (XSS escaping, bold promotion, empty path, filename extension, bytes encoding)"
      contains: "def test_"
  key_links:
    - from: "tests/test_html_export.py"
      to: "src/utils.py"
      via: "import build_html_report, html_report_to_bytes, generate_export_filename"
      pattern: "from src.utils import"
---

<objective>
Capture the ALREADY-COMPLETE "EXPORT HTML" feature and lock it with a regression test, then commit all uncommitted work atomically.

The implementation exists in the working tree (uncommitted): `src/utils.py` gained `build_html_report`, `_summary_to_html`, `html_report_to_bytes`, and a generalized `generate_export_filename(prefix, extension="csv")`; `app.py` wired an EXPORT HTML download button beside EXPORT CSV in a `st.columns(2)` layout, threaded `question`/`provider`/`model`/`route` through `display_results` and both call sites, and added `"question"` + `"route"` to the assistant message dict and the `process_query` return dict.

This is a CAPTURE/DOCUMENTATION task, not a from-scratch build. The single executable task adds the missing regression test for the new helpers; the executor then commits everything together.

Purpose: Lock the XSS-escaping and backward-compatibility guarantees of the HTML export helpers against silent drift, and land the feature as a clean commit.
Output: `tests/test_html_export.py` and an atomic commit of all related uncommitted changes.

NON-worktree run: changes pre-exist in the main working tree. Do NOT create a worktree or re-implement `app.py`/`src/utils.py` — they are done. Only add the test and commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
<!-- As-built signatures in src/utils.py — executor uses these directly, no exploration needed. -->

From src/utils.py:
```python
def generate_export_filename(prefix: str = "incidents", extension: str = "csv") -> str
    # returns f"{prefix}_{YYYYMMDD_HHMMSS}.{extension.lstrip('.')}"

def build_html_report(
    question: str,
    df: pd.DataFrame,
    executive_summary: str | None = None,
    *,
    sql: str | None = None,
    provider: str | None = None,
    model: str | None = None,
    route: str | None = None,
) -> str
    # Self-contained HTML doc. Every dynamic value HTML-escaped via html.escape.
    # Empty/None df -> '<p class="report-empty">No matching incidents were found.</p>'
    # df.to_html(..., escape=True) escapes cell values.
    # _summary_to_html(): escape() first, then **bold** -> <strong>, blank-line-split -> <p>.

def html_report_to_bytes(html: str) -> bytes
    # html.encode("utf-8")
```

Note: `_summary_to_html` escapes the summary BEFORE promoting `**...**`, so injected
`<script>` in the summary survives as `&lt;script&gt;` while `**Findings**` becomes
`<strong>Findings</strong>`. The report title and `<h1>` also escape the question.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression test for HTML-export helpers, then verify full suite</name>
  <files>tests/test_html_export.py</files>
  <behavior>
    - XSS / escaping: build_html_report with question="&lt;script&gt;alert(1)&lt;/script&gt;",
      a one-row df whose cell contains "&lt;img src=x onerror=1&gt;", and an
      executive_summary containing "&lt;b&gt;raw&lt;/b&gt;" produces HTML where NO raw
      "&lt;script&gt;" (literal tag) appears, and the escaped form "&amp;lt;script&amp;gt;"
      IS present. (Use the literal characters in the test; the entity forms are what
      must appear in output.)
    - Bold promotion: executive_summary="**Key Findings**: stable" yields
      "<strong>Key Findings</strong>" in the output (and no literal "**Key Findings**").
    - Empty df path: build_html_report("q", pd.DataFrame()) returns a str containing
      "No matching incidents were found." and raises no exception.
    - Filename extension: generate_export_filename("incident_report", "html") ends with
      ".html"; generate_export_filename("incidents") (default) ends with ".csv"
      (backward-compat lock).
    - Bytes: html_report_to_bytes("<html></html>") returns an instance of bytes that
      decodes back to the input.
  </behavior>
  <action>Create tests/test_html_export.py following the conventions of tests/test_phase5_ui.py — module docstring stating run contract `PYTHONPATH=. python -m pytest tests/ -q`, `from __future__ import annotations`, inline setup (NO fixture files, NO live deps), one assertion-focused function per behavior. Import the helpers with `from src.utils import build_html_report, html_report_to_bytes, generate_export_filename` and `import pandas as pd`. Implement exactly the five behaviors above. For the XSS test, construct the injected strings using the literal `<script>` / `<img ...>` / `<b>` characters as INPUT, then assert the literal opening tag `"<script>"` is NOT a substring of the returned HTML while the escaped entity `"&lt;script&gt;"` IS present (the report's own CSS/markup legitimately contains other `<` characters, so assert on the specific injected tag, not on a blanket absence of `<`). For the bold test, assert `"<strong>Key Findings</strong>"` is present and `"**Key Findings**"` is absent. Do NOT modify src/utils.py or app.py — the implementation is complete and correct; this task only adds the lock. After writing, run the full suite to confirm no regression.</action>
  <verify>
    <automated>cd C:\mbrunoapp\snow_query; $env:PYTHONPATH="."; python -m pytest tests/test_html_export.py -q; python -m pytest tests/ -q</automated>
  </verify>
  <done>tests/test_html_export.py exists with five passing test functions; the full `python -m pytest tests/ -q` suite is green (no regressions in the prior ~103 tests).</done>
</task>

</tasks>

<verification>
- `PYTHONPATH=. python -m pytest tests/test_html_export.py -q` passes (5 tests).
- `PYTHONPATH=. python -m pytest tests/ -q` passes with no regressions.
- No changes were made to src/utils.py or app.py during this task (capture-only).
</verification>

<success_criteria>
- A regression test locks: HTML escaping of question/cells/summary, `**bold**`→`<strong>`, empty-df no-results path, the `.html`/`.csv` filename-extension contract (backward compat), and `html_report_to_bytes` returning bytes.
- All uncommitted work (app.py, src/utils.py, tests/test_html_export.py) is committed atomically by the executor.
- Full test suite green.
</success_criteria>

<output>
Create `.planning/quick/260621-aj0-html-export-query-report/260621-aj0-SUMMARY.md` when done.

Commit everything atomically (NON-worktree; changes pre-exist in the main working tree):
`git add app.py src/utils.py tests/test_html_export.py && git commit` with a message such as
`feat(export): add self-contained Loro Piana HTML report export + regression test`.
</output>
