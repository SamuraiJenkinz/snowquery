---
phase: quick-260622-7t4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils.py
  - app.py
  - tests/test_html_export.py
autonomous: true
requirements: [QUICK-260622-7t4]

must_haves:
  truths:
    - "An EXPORT SUMMARY button appears beside EXPORT CSV / EXPORT HTML when an executive summary exists"
    - "The summary-only HTML report contains the question + executive summary + provenance and omits the results table and Generated SQL"
    - "The full HTML report is unchanged (still includes results table and SQL)"
    - "Full test suite passes 121/121"
  artifacts:
    - path: "src/utils.py"
      provides: "build_html_report keyword-only summary_only flag"
      contains: "summary_only"
    - path: "app.py"
      provides: "EXPORT SUMMARY button wired in display_results"
      contains: "EXPORT SUMMARY"
    - path: "tests/test_html_export.py"
      provides: "3 summary_only tests + full-report regression guard"
      contains: "test_summary_only_omits_results_and_sql_keeps_summary"
  key_links:
    - from: "app.py display_results"
      to: "src/utils.py build_html_report"
      via: "summary_only=True kwarg"
      pattern: "summary_only=True"
---

<objective>
Capture and commit the already-implemented "EXPORT SUMMARY" feature: a summary-only HTML report (question + executive summary + provenance; no results table, no SQL) offered beside EXPORT CSV / EXPORT HTML.

Purpose: The implementation is COMPLETE and uncommitted in the working tree. This is a documentation/capture task — verify the test suite is green and commit atomically. Do NOT re-implement.

Output: A single atomic commit capturing src/utils.py, app.py, and tests/test_html_export.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# As-built (already in working tree — verify, do NOT re-implement):
# - src/utils.py build_html_report(...) gained keyword-only `summary_only: bool = False`.
#   When True: omits Results + Generated SQL sections, keeps question + executive summary
#   + provenance; if no summary exists emits an explicit "No executive summary was
#   generated" note. Full-report path unchanged (results/SQL refactored into locals).
# - app.py display_results: export row is st.columns(3 if _has_summary else 2);
#   col[0]=EXPORT CSV, col[1]=EXPORT HTML (full), col[2]=EXPORT SUMMARY (only when
#   executive_summary truthy), calls build_html_report(..., summary_only=True),
#   filename prefix `incident_summary`, "html" extension, key export_summary_{query_id}.
# - tests/test_html_export.py: +3 tests (summary_only omits results/SQL keeps summary;
#   summary_only without summary states absence + valid doctype; full report still
#   includes results + SQL regression guard).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify suite green and commit atomically</name>
  <files>src/utils.py, app.py, tests/test_html_export.py</files>
  <action>
This is a CAPTURE task — the feature is already implemented in the working tree. Do NOT edit source or test files; do NOT re-implement.

Step 1 — Verify the targeted export tests pass:
Run `python -m pytest tests/test_html_export.py -q`. All tests including the 3 new summary_only tests (test_summary_only_omits_results_and_sql_keeps_summary, test_summary_only_without_summary_states_absence, test_full_report_still_includes_results_and_sql) must pass.

Step 2 — Verify the full suite is green:
Run `python -m pytest -q`. Expect exactly 121 passed. If the count differs or any test fails, STOP and report — do NOT attempt to fix by editing code; surface the failure for the orchestrator.

Step 3 — Commit atomically (commit only; do NOT push — the orchestrator handles push, and this repo's only remote is `samurai`):
Stage exactly the three changed files and commit:
`git add src/utils.py app.py tests/test_html_export.py`
`git commit -m "feat(export): add summary-only HTML report export button" -m "EXPORT SUMMARY produces a summary-only report (question + executive summary + provenance; no results table, no SQL), shown beside EXPORT CSV/HTML when an executive summary exists. build_html_report gains keyword-only summary_only flag; +3 tests (121/121 green)." -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"`
  </action>
  <verify>
    <automated>python -m pytest -q</automated>
  </verify>
  <done>Full suite reports 121 passed; the three files are committed in a single atomic commit; nothing pushed.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_html_export.py -q` passes (includes 3 summary_only tests).
- `python -m pytest -q` reports 121 passed.
- `git log -1 --stat` shows a single commit touching exactly src/utils.py, app.py, tests/test_html_export.py.
- `git status` is clean for those files; no `git push` was executed.
</verification>

<success_criteria>
- Summary-only export feature captured in one atomic commit.
- 121/121 tests green.
- No push performed (orchestrator owns push; only remote is `samurai`).
</success_criteria>

<output>
Create `.planning/quick/260622-7t4-summary-only-export-button/260622-7t4-SUMMARY.md` when done.
</output>
