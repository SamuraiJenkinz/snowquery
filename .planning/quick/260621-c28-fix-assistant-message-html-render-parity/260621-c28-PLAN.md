---
phase: quick-260621-c28
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app.py, tests/test_render_parity.py]
autonomous: true
requirements: [QUICK-C28]
quick_id: 260621-c28
non_worktree: true

must_haves:
  truths:
    - "Assistant-message HTML (route-badge / results-count <span>s) renders correctly on every Streamlit rerun, not just the first"
    - "Both render sites (live + history) pass unsafe_allow_html=True"
    - "A regression test AST-locks render parity so the divergence cannot recur silently"
  artifacts:
    - path: "app.py"
      provides: "History-render st.markdown(message['content']) with unsafe_allow_html=True (line ~566) matching live path (~line 928)"
      contains: "unsafe_allow_html=True"
    - path: "tests/test_render_parity.py"
      provides: "AST-walk regression test asserting every st.markdown(<name>['content']) passes unsafe_allow_html=True"
      contains: "test_all_content_render_sites_pass_unsafe_allow_html_true"
  key_links:
    - from: "tests/test_render_parity.py"
      to: "app.py"
      via: "ast.parse of app.py source"
      pattern: "ast\\.parse"
---

<objective>
Capture and commit the assistant-message HTML render-parity fix (already applied to the working tree, uncommitted).

Purpose: The assistant message content is app-built HTML (route-badge / results-count `<span>`s from process_query). It is rendered in TWO divergent sites: the live path in render_main_content (`st.markdown(response["content"], unsafe_allow_html=True)`, ~line 928 — already correct) and the history re-render path in render_chat_history (~line 566). The history path previously called `st.markdown(message["content"])` WITHOUT the flag, so badges looked correct when first produced but turned into literal `<span>` markup on the next Streamlit rerun.

Root cause: two divergent render sites for the same content, only one passing unsafe_allow_html=True. Prevention: an AST-lock regression test that fails if any `st.markdown(<name>["content"])` call (name in {message, response}) omits `unsafe_allow_html=True`.

This is a CAPTURE task — the fix and test pre-exist on the main tree. Do NOT re-implement. The single task is verify + atomic commit.

Output: Committed app.py fix + tests/test_render_parity.py regression lock.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@app.py
@tests/test_render_parity.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify render-parity fix and commit atomically</name>
  <files>app.py, tests/test_render_parity.py</files>
  <action>
Do NOT modify app.py or tests/test_render_parity.py — the fix is already applied on the working tree. Verify, then commit.

1. Run the targeted regression test: `PYTHONPATH=. python -m pytest tests/test_render_parity.py -q` (PowerShell: `$env:PYTHONPATH="."; python -m pytest tests/test_render_parity.py -q`). Both tests MUST pass (test_content_render_sites_exist finds >=2 content render sites; test_all_content_render_sites_pass_unsafe_allow_html_true confirms every site passes unsafe_allow_html=True).
2. Run the full suite: `PYTHONPATH=. python -m pytest tests/ -q`. All tests MUST pass (expected 110 passed).
3. If any test fails, STOP and report — do not commit. The capture assumes a green tree; a failure means the working-tree state diverged from expectations.
4. Commit only the two fix files atomically with message documenting root cause (two divergent render sites — live correct, history missing the flag) and the AST-lock prevention.
  </action>
  <verify>
    <automated>cd C:\mbrunoapp\snow_query; $env:PYTHONPATH="."; python -m pytest tests/test_render_parity.py tests/ -q</automated>
  </verify>
  <done>tests/test_render_parity.py passes (2 tests); full suite passes (110); app.py + tests/test_render_parity.py committed in a single commit referencing the root cause and AST-lock prevention.</done>
</task>

</tasks>

<verification>
- `PYTHONPATH=. python -m pytest tests/test_render_parity.py -q` → 2 passed
- `PYTHONPATH=. python -m pytest tests/ -q` → 110 passed
- `git log -1 --stat` shows app.py and tests/test_render_parity.py in the latest commit
</verification>

<success_criteria>
- History-render path passes unsafe_allow_html=True, matching the live-render path
- AST-lock regression test green and committed
- Full suite green (110 passed)
- Single atomic commit captures the fix + test with root-cause documentation
</success_criteria>

<output>
Create `.planning/quick/260621-c28-fix-assistant-message-html-render-parity/260621-c28-SUMMARY.md` when done
</output>
